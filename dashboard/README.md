# Dashboard

Next.js app for visualizing DA protocol benchmarking results.

## Setup

```bash
cd dashboard
npm install
npm run dev        # http://localhost:3000
```

## Data

The `data/` directory contains JSON files consumed by the charts. Currently dummy data — will be replaced with real results.

## Stack

Next.js 15, TypeScript, Tailwind CSS, Recharts.
